services:

  db:
    image: postgres
    restart: always
    shm_size: 128mb
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: nina
      POSTGRES_PASSWORD: password
      POSTGRES_DB: classroom
    volumes:
      - ./data:/var/lib/postgresql
     


